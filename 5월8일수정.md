# Compact SQIsign — Implementation (private)

본 레포는 "Compact Quaternion Algorithms for SQIsign" (Won Kim, Changmin Lee, Hyunwoo Yoo) 의 **구현 통합 워크스페이스** 입니다.
변경 사항은 fix_bundle.zip 파일에 임시로 업로드

Base: `munsanwon2/SQIsign-Fixed-Precision` (KLKL25 fixed-precision, GMP-free).
통합 대상: `yhwkorea/the-sqisign feat/mlll-ideal-operations` 의 MLLL 자산 (3저자 로컬에서 작업 중, 곧 PR 으로 push 예정).

## 표준 빌드 명령어

```bash
mkdir -p build
cd build
cmake -DSQISIGN_BUILD_TYPE=ref ..
make
make test
```

→ **`make` 까지 100% 성공**. `make test` 는 단위 테스트 18/36 통과. 19-36 은 Windows MinGW 환경 이슈 (Linux/WSL 에서 재실행 필요).

## 1저자 요청 (2026-05-08 카톡)

> "수정하면 얘기해줄게 일단은 기존 bound 로 correctness 만 테스트 하고 레벨별로 통합하는거 (build 할 때 에러 뜨는거 수정) 하고 있어줘"

→ 본 commit 이 그 build 에러 수정에 해당. KLKL25 baseline 자체가 표준 명령으로 깔끔하게 빌드 안 됐던 두 가지 버그를 fix.

## Baseline build 수정 사항 (이번 commit)

### 1. `intbig.h` preprocessor 버그 fix

기존 코드:
```c
#define SQISIGN_LVL1 lvl1
#define SQISIGN_LVL3 lvl3
#define SQISIGN_LVL5 lvl5

#if SQISIGN_VARIANT == SQISIGN_LVL1
#define IBZ_LIMBS 110
#elif SQISIGN_VARIANT == SQISIGN_LVL3
#define IBZ_LIMBS 168
#elif SQISIGN_VARIANT == SQISIGN_LVL5
#define IBZ_LIMBS 222
```

**버그**: C preprocessor 의 `#if A == B` 에서 비숫자 token (`lvl1`, `lvl3` 등) 은 모두 0 으로 평가됨. 즉 `lvl1 == lvl1` → `0 == 0` → **세 분기 모두 true**. 첫 분기만 매치되어 **모든 레벨 빌드에서 IBZ_LIMBS = 110 가 됨**. 그러나 L3 precomp 데이터 (`src/precomp/ref/lvl3/quaternion_data.c`) 는 168 limbs 가정으로 생성되어 **"excess elements in array initializer" 빌드 에러** 발생.

**Fix**: token concatenation 으로 per-level 매크로 직접 선택. CMake 변경 없음, intbig.h 한 곳만 수정.

```c
#define _IBZ_CONCAT(a, b) a##b
#define _IBZ_CONCAT2(a, b) _IBZ_CONCAT(a, b)

#define IBZ_LIMBS_lvl1 110
#define IBZ_LIMBS_lvl3 168
#define IBZ_LIMBS_lvl5 222

#ifdef SQISIGN_VARIANT
#define IBZ_LIMBS _IBZ_CONCAT2(IBZ_LIMBS_, SQISIGN_VARIANT)
#else
#define IBZ_LIMBS 222
#endif
```

`SQISIGN_VARIANT=lvl3` → `IBZ_LIMBS_lvl3` → `168` (정상 expansion).

### 2. `.cmake/flags.cmake` — GCC 14 false-positive 경고 2개 error 에서 제외

GCC 14 가 strict 모드에서 코드 실제 정확함에도 다음 두 false-positive 발생:

- `mp.c:297` — `x[0] ^= (1<<1)` 에서 `maybe-uninitialized` (직전 line 의 `mp_add(x, ...)` 추적 실패)
- `apps/fuzz_verify.c:26` — `snprintf("%s.bin", ...)` 에서 `format-truncation` (overflow 가능 false positive)

**Fix**: 이 두 가지 warning 만 `-Wno-error=` 로 error 에서 제외. 다른 strict 옵션은 그대로 유지 (`-Wall`, `-Wextra`, `-Werror` 자체).

```cmake
if(CMAKE_C_COMPILER_ID STREQUAL "GNU")
    set(STRICT_OPTIONS_C "${STRICT_OPTIONS_C} -Wno-error=maybe-uninitialized -Wno-error=format-truncation")
endif()
```

### 결과

`-DENABLE_STRICT=OFF` 같은 우회 옵션 없이 **표준 명령어 그대로 100% 빌드 성공**.

## 테스트 결과

`make test` (= ctest) 36 케이스, 환경: Windows 11 + MinGW UCRT64 GCC 14.2.0:

| 테스트 그룹 | 결과 |
|---|---|
| 1-18 quaternion / gf / curve_arith / ec_biextension / ec_basis_gen (3 levels each) | ✅ **18/18 통과** |
| 19-21 sqisign_test_id2iso_lvl{1,3,5} | ❌ SEGFAULT |
| 22-27 signature / threadsafety (3 levels) | ❌ SEGFAULT / 0xC0000409 |
| 28-30 nistapi (3 levels) | ❌ SEGFAULT / 0xC0000409 |
| 31-36 KAT / SELFTEST (3 levels) | ❌ SEGFAULT / 0xC0000409 |

→ **단위 산술/구조 레벨은 다 통과**. 상위 레벨 (id2iso, signature, full protocol) 은 Windows MinGW 환경 자체 이슈 (`STATUS_STACK_BUFFER_OVERRUN` 0xC0000409 = GCC stack-protector canary 트립, 또는 SEGFAULT). IBZ_LIMBS 와 무관.

→ **Linux/WSL 에서 재실행 필요** (Repo 2 가 Linux 검증 환경 베이스). 1저자가 Linux 환경 갖고 있으면 위 표준 명령어로 19-36 도 통과 가능성 높음.

## 다음 단계 (TODO)

- [ ] **Linux/WSL 환경에서 `make test` 19-36 통과 확인** — Windows-specific 이슈 분리
- [ ] 3저자 로컬 (`C:\sqi\base\`) 의 MLLL 통합 작업물을 PR 단위로 push:
  - PR1: GCD/det 변종 알고리즘 (`ibz_gcdext_alt.c`, `ibz_det_alt.c`, `ibz_det_bareiss.c`) + 테스트 — 핫패스 영향 0
  - PR2: MLLL 본체 (`mlll.c`, `mlll_gram.c`, `mlll_internals.h`, `quat_fixed_precision.c`) 도입
  - PR3: macro alias 라우팅 (`quaternion.h` 끝 블록, top-level `CMakeLists.txt` `SQISIGN_USE_MLLL_GRAM` option)
  - PR4: `lll_applications_mlll_gram.c` (4 MLLL 변종 본체)
  - PR5: `REPORT_TO_FIRST_AUTHOR.md` — 페이퍼 §5 분석 갭에 대한 정량 보고
- [ ] 1저자 review 후 페이퍼 bound (1774/2696/3555 bit) 관련 옵션 결정
  - 옵션 A: 페이퍼 §5 분석 보강 (각 보조 산술 explicit lemma)
  - 옵션 B: implementation-realized bound 약간 상향 + Table 1 footnote
  - 옵션 C: claim 범위 명확화 (MLLL bottleneck 한정)

## 페이퍼 §5 분석 갭 (요약)

상세 분석은 `REPORT_TO_FIRST_AUTHOR.md` (PR 으로 곧 push) 에 정리. TL;DR:

- 페이퍼 1774/2696/3555 bit 는 **MLLL 본체 정수 bound** 에 valid
- 그러나 SQIsign 구현의 **보조 산술** (`ibz_gcdext` Euclidean intermediate, `ibz_mat_4x4_inv_with_det_as_denom` 의 4×4 det Hadamard) 이 페이퍼 bound 위 ~1.3x 까지 transient 발생
- §5 의 "for other lines, follow [KLKL25, Theorem 1]" outsource 가 KLKL25 7026 bit 마진에 의존하는데 우리 1774 bit 에서 그 마진이 없음
- 실측: Hadamard 헤드룸 포함 ~2240/3400/4500 bit (옵션 B) 가 honest 한 implementation-wide bound. KLKL25 대비 여전히 ~68% 절감.

## 협업 워크플로

- main 직접 push 없이 모든 변경은 feature branch + PR
- 큰 논의는 Issues 로
- 카톡/이메일 동기 논의 후 결정 사항을 PR/Issue 코멘트에 남겨 추적 가능하게

## 빌드 환경 참고 (3저자 로컬)

- Windows 11 Enterprise x64
- portable CMake 3.30.5 + winlibs MinGW UCRT64 GCC 14.2.0
- 빌드 옵션: `-DSQISIGN_BUILD_TYPE=ref` (위 수정 후 다른 옵션 불필요)

Linux/WSL 환경에서도 같은 명령어로 빌드 가능. GCC 11/12 에선 strict warning 도 발생 안 할 수 있음.

---

*마지막 갱신: 2026-05-08, 3저자 (Hyunwoo Yoo)*
*변경: intbig.h preprocessor fix, flags.cmake GCC 14 호환, README 갱신*
